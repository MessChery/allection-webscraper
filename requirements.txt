# Allection web scraping microservice — Python dependencies
# Pin minor versions for reproducible builds; bump intentionally.

# ── Web framework & server ────────────────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.3        # [standard] pulls in uvloop + httptools

# ── HTTP client (async TLS-impersonating client, used by ScraperClient) ──
curl_cffi>=0.16.0

# ── HTML parsing (used by FnacPtScraper) ─────────────────────────────────
beautifulsoup4==4.13.4
lxml==5.3.2

# ── Data validation (used by ScrapedItem model) ───────────────────────────
pydantic==2.13.5
